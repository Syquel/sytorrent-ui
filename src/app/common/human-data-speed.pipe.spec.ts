import { HumanDataSpeedPipe } from './human-data-speed.pipe';

describe('HumanDataSpeedPipe', () => {
  it('create an instance', () => {
    const pipe = new HumanDataSpeedPipe();
    expect(pipe).toBeTruthy();
  });
});
