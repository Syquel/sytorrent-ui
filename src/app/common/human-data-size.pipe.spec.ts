import { HumanDataSizePipe } from './human-data-size.pipe';

describe('HumanDataSizePipe', () => {
  it('create an instance', () => {
    const pipe = new HumanDataSizePipe();
    expect(pipe).toBeTruthy();
  });
});
